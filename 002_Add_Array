/*Using C Programming , Add numbers of two EQUAL size integer arrays. You must use for loops to add the arrays and indexing 

Example :

1) array1 is size 3 and has values 2,4,6 ==>  array1[0] = 2; array1[1]=4; array1[2] = 6;

2) array2 is size 3 and has values 2,3,5  ==> array2[0] = 2; array2[1] = 3; array2[2] = 5;

Then : sum_array[0] should be 4, sum_array[1] should be 7 and sum_array[2] should be 11*/


#include <stdio.h>
int main ()
{
    int i = 0 ;
    // initialize arrays
    int array1[3]={2,4,6};
    int array2[3]={2,3,5};
    int total[3]={0,0,0};
    //Print Values
    printf("Array 1 Values: %d %d %d\n",array1[0],array1[1],array1[2]);
    
    printf("Array 2 Values: %d %d %d\n",array2[0],array2[1],array2[2]);
// For Loops to add two arrays
    for (i=0; i<3; i++)
    {
        total[i]=array1[i]+array2[i];
        printf("%d\n",total[i]);
    }
return 0;
}
